export const absolutePath = {
	"name": "HTTP server",
	"exec": "node server.js",
	"workingDir": __dirname,
	"env": { },
}

export const relativePath = {
	"name": "HTTP server",
	"exec": "node server.js",
	"workingDir": ".",
	"env": { },
}
